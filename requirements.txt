# Django and its dependencies
Django==3.2.7
django-environ==0.4.5
django-cors-headers==3.13.0
whitenoise==6.2.0

# Web framework tools
gunicorn==20.1.0

# Utility packages
requests==2.26.0

# Development and debugging (optional)
django-debug-toolbar==3.2.2

# C++ build uses Conan 2.x separately — install with:
#   python3 -m pip install "conan>=2.0" cmake
